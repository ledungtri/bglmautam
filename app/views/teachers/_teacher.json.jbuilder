json.extract! teacher, :id, :christian_name, :full_name, :named_date, :date_birth, :occupation, :phone, :email, :street_number, :street_name, :ward, :district, :created_at, :updated_at
json.url teacher_url(teacher, format: :json)
