<%
  actions = [
    { label: 'In Danh Sách GLV', path: teachers_path(format: :pdf) },
    { label: 'In Bảng Tự Tạo', path: teachers_custom_export_path }
  ]
%>

<%= render 'info_table', guidances: @guidances %>
<%= render 'components/button_group', heading: 'In Ấn', actions: actions %>
