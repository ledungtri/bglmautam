<%= render 'components/form/form', object: @student, heading: 'Thông Tin Thiếu Nhi' %>
<%= render 'components/button', button: { label: 'In Sơ Yếu Lý Lịch', path: student_path(format: "pdf") } if @student.id %>
<%= render 'enrollments/index' %>