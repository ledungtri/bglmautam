<%= render 'components/form/form',
           heading: 'Kết Quả Học Tập',
           object: @enrollment,
           editable: @current_user.admin_or_teacher_of_enrollment?(@enrollment, @current_year)
%>
<%= render 'components/form/form',
           heading: 'Nhận Xét Quá Trình',
           object: @enrollment.evaluations.first_or_initialize,
           editable: @current_user.admin_or_teacher_of_enrollment?(@enrollment, @current_year)
%>